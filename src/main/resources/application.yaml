spring:
  application:
    name: invoice-service

  datasource:
    url: jdbc:postgresql://localhost:5432/ai_invoice_management
    username: admin
    password: 12345678
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true